# Roadmap
#### GH Actions
- [X] Make release changes dynamic; read from CHANGELOG.md
- Sort uploaded artifacts by platform and arch name
- Build and release packages for each arch (same way aether is)
- Ship each package with it's platform specific aether binary

#### UI/UX
- [X] Use the icons under `asset/icon`
- Use AetherPrism instead of aether_prism as the app and package name
- Include a filepicker for choosing aether binary path
  - Remove the above option and use the shipped aether binary instead
- Put control panel (Start, Stop, etc) above the config options (treat them as advanced options)

