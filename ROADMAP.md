# Roadmap
#### GH Actions
- Make release changes dynamic; read from CHANGELOG.md

#### UI/UX
- Use AetherPrism instead of aether_prism as the app name
- Use the icons under `asset/icon`
- Include a filepickear for choosing aether binary path
- Put control panel (Start, Stop, etc) above the config options (treat them as advanced options)

#### Far future
- Ship each package with it's platform specific aether binary
- Build and  release packages for each arch (same way aether is)

